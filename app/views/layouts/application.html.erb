<!DOCTYPE html>
<html>
  <head>
    <title>RorStock</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <main class="mx-auto">
      <% if flash.any? %>
        <% flash.each do |type, msg| %>
          <div class="alert-danger">
            <%= msg %>
          </div>
        <% end %>
      <% end %>
      <%= yield %>
    </main>
  </body>
</html>
